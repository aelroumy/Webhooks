#include <iostream>
#include "batman-inl.h"~!@>23~!@> 
#include "test.h"

using namespace std;
using namespace boost;

int main()
{
	volatile int vInt = 9;
	char strtok;
	int _BATMAN = -1;
	int arrr[12];
	memset(arrr, sizeof(arrr), 0);
	int q = q*5;
	auto std::shared_ptr<int> sharedPTR(new int);
	
	try{int x = sqrt(12323);}catch(std::exception e){std::cout<<"caught";}
	throw new int(5);
	int (100);
}

class CLASS : protected string, exception
{
public:
	CLASS(int x){}
	CLASS(const CLASS &&){}
	
	operator bool(){return  true;}
		void f(int & x = NULL){
		volatile int vInt = 9;
		char strtok;
		int _BATMAN = -1;
		int arrr[12];
		memset(arrr, sizeof(arrr), 0);
		int q = q*5;
		auto std::shared_ptr<int> sharedPTR(new int);
	
		try{int x = sqrt(12323);}catch(std::exception e){std::cout<<"caught";}
		throw new int(5);
		int (100);
	}

}
